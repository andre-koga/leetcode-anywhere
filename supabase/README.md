# Supabase (future)

Placeholder for the future Supabase project: migrations, edge functions, and
generated types will live here once online sync of solutions/submissions is added.

Planned tables (mirroring the local Dexie schema in `frontend/src/db/`):

- `drafts` - per user, per problem, per language saved code
- `submissions` - submission history with results

Initialize later with `supabase init` from this directory.
